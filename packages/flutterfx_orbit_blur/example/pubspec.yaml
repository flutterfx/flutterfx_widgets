name: flutterfx_orbit_blur_example
description: Demonstrates how to use the flutterfx_orbit_blur package.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutterfx_orbit_blur:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
