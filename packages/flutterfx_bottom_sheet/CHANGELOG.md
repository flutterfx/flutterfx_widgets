## 1.0.1

* Fix README preview image not displaying on pub.dev (use absolute raw GitHub URL).

## 1.0.0

* Initial release.
* `FxBottomSheet` widget with scale-and-slide entrance animation.
* `FxBottomSheetController` for programmatic open / close / toggle.
* `FxBottomSheetStyle` with `light()` and `dark()` presets and `copyWith()`.
* Drag-to-dismiss with velocity-aware snapping.
